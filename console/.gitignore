# compiled output
# NOTA: /dist/ está excluido por defecto (modo build-en-VM).
# Para usar el modo pre-built (build local + commit del dist), ejecuta:
#   bash scripts/build-console.sh
# y luego comenta la línea /dist/ antes de hacer git add.
/dist/
/declarations/

# dependencies
/node_modules/
/scripts/node_modules/

# misc
/.env*
/environments/.env*
/.pnp*
/.eslintcache
/coverage/
/npm-debug.log*
/testem.log
/yarn-error.log
/.npmrc

# ember-try
/.node_modules.ember-try/
/npm-shrinkwrap.json.ember-try
/package.json.ember-try
/package-lock.json.ember-try
/yarn.lock.ember-try

# broccoli-debug
/DEBUG/

# Backups de docker-compose override (timestamped)
docker-compose.override.yml.bak.*
