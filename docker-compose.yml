services:
  cache:
    image: redis:4-alpine
    restart: unless-stopped
    mem_limit: 64m
    mem_reservation: 32m
    pids_limit: 100
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  database:
    image: mysql:8.0-oracle
    restart: always
    mem_limit: 800m
    mem_reservation: 400m
    pids_limit: 512
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 4096
   # ports:
   #   - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: "KanTrack25*"
      MYSQL_DATABASE: "trackbase"
    volumes:
      - ./docker/database/:/docker-entrypoint-initdb.d/
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    stop_grace_period: 60s

  socket:
    image: socketcluster/socketcluster:v17.4.0
    restart: unless-stopped
    mem_limit: 128m
    mem_reservation: 64m
    pids_limit: 256
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 4096
    ports:
      - "38000:8000"
    environment:
      SOCKETCLUSTER_WORKERS: 1
      SOCKETCLUSTER_BROKERS: 1

  scheduler:
    image: local-fleetbase-api:latest
    command: ["go-crond", "--verbose", "root:./crontab"]
    restart: unless-stopped
    mem_limit: 96m
    mem_reservation: 48m
    pids_limit: 128
    tmpfs:
      - /tmp:size=64m
    healthcheck:
      test: ["CMD", "pgrep", "-x", "go-crond"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    environment:
      DATABASE_URL: "mysql://root:KanTrack25*@database/trackbase"
      QUEUE_CONNECTION: redis
      CACHE_DRIVER: redis
      CACHE_PATH: /fleetbase/api/storage/framework/cache
      CACHE_URL: tcp://cache
      REDIS_URL: tcp://cache
    depends_on:
      - database
      - cache

  queue:
    image: local-fleetbase-api:latest
    command: ["php", "artisan", "queue:work", "--sleep=2", "--tries=3", "--memory=128", "--max-jobs=50", "--max-time=900"]
    restart: unless-stopped
    mem_limit: 384m
    mem_reservation: 192m
    pids_limit: 256
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 4096
    tmpfs:
      - /tmp:size=128m
    healthcheck:
      test: ["CMD", "php", "artisan", "queue:status"]
      interval: 30s
      timeout: 10s
      retries: 3
    environment:
      DATABASE_URL: "mysql://root:KanTrack25*@database/trackbase"
      QUEUE_CONNECTION: redis
      CACHE_DRIVER: redis
      CACHE_PATH: /fleetbase/api/storage/framework/cache
      CACHE_URL: tcp://cache
      REDIS_URL: tcp://cache
      MAIL_FROM_NAME: "Fleetbase"
      MAIL_MAILER: "smtp"
      MAIL_HOST: "sandbox.smtp.mailtrap.io"
      MAIL_PORT: 2525
      MAIL_USERNAME: "79e8629fd3fb41"
      MAIL_PASSWORD: "b0e9c1b266e2c0"
      MAIL_ENCRYPTION: "tls"
      MAIL_FROM_ADDRESS: "from@example.com"
    depends_on:
      - database
      - cache

  console:
    build:
      context: .
      dockerfile: ./console/Dockerfile
      args:
        ENVIRONMENT: production
    ports:
      - "4200:4200"
    volumes:
      # IMPORTANTE: El archivo debe existir en esta ruta
      - ./console/fleetbase.config.json:/usr/share/nginx/html/fleetbase.config.json:ro
    environment:
      # Variables para depuración (interno, no dominio público)
      API_HOST: "http://application:8000"
      SOCKET_HOST: "socket"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4200/health.json"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    depends_on:
      application:
        condition: service_healthy
      socket:
        condition: service_started
    restart: unless-stopped
    mem_limit: 128m
    mem_reservation: 64m
    pids_limit: 128
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 2048
    tmpfs:
      - /tmp:size=64m
    networks:
      - default

  application:
    image: local-fleetbase-api:latest
      # command: php artisan serve --host=0.0.0.0 --port=8000
      #  - ./api/.env:/fleetbase/api/.env
      # - ./api:/fleetbase/api:cached
      # - ./packages/fleetops:/fleetbase/api/vendor/fleetbase/fleetops-api
    environment:
      # URL pública detrás de Nginx + SSL
      APP_URL: "https://kantrack.tech"
      #ENVIRONMENT: "production"

      DATABASE_URL: "mysql://root:KanTrack25*@database/trackbase"
      QUEUE_CONNECTION: redis
      CACHE_DRIVER: redis
      CACHE_PATH: /fleetbase/api/storage/framework/cache
      CACHE_URL: tcp://cache

      # Dónde vive la consola vista por el usuario (dominio público)
      CONSOLE_HOST: "https://kantrack.tech"
      FRONTEND_HOSTS: "https://kantrack.tech"

      REDIS_URL: tcp://cache

      # Dominio de sesión para cookies
      SESSION_DOMAIN: "kantrack.tech"

      BROADCAST_DRIVER: "socketcluster"
      MAIL_FROM_NAME: "Fleetbase"
      MAIL_MAILER: "smtp"
      MAIL_HOST: "sandbox.smtp.mailtrap.io"
      MAIL_PORT: 2525
      MAIL_USERNAME: "79e8629fd3fb41"
      MAIL_PASSWORD: "b0e9c1b266e2c0"
      MAIL_ENCRYPTION: "tls"
      MAIL_FROM_ADDRESS: "from@example.com"
      APP_NAME: "Fleetbase"
      LOG_CHANNEL: "daily"
      REGISTRY_HOST: "https://registry.fleetbase.io"
      REGISTRY_PREINSTALLED_EXTENSIONS: "true"
      OSRM_HOST: "https://router.project-osrm.org"
    depends_on:
      - database
      - cache
      - queue
    networks:
      - default
    restart: always
    mem_limit: 768m
    mem_reservation: 384m
    pids_limit: 512
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 4096
    tmpfs:
      - /tmp:size=128m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  httpd:
    build:
      context: .
      dockerfile: docker/httpd/Dockerfile
    ports:
      - "8000:80"
    depends_on:
      - application
    mem_limit: 96m
    mem_reservation: 48m
    pids_limit: 100
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
      nproc: 2048
    tmpfs:
      - /tmp:size=64m

networks:
  default:
    driver: bridge

volumes:
  mysql_data:
    driver: local
